@extends('development.layout')
@section('title','My Bookings & Connections')
@section('content')<div class="page-heading"><div><h1>My Bookings & Connections</h1><p>Follow your family’s support requests.</p></div><a class="button" href="{{ route('development.providers') }}">Find Providers →</a></div>@forelse($connections as $connection)<article class="card programme-row"><div><h2>{{ $connection->provider->user->name }}</h2><p>{{ $connection->child->name }} · Requested {{ $connection->created_at->format('d M Y') }}</p><p>Awaiting follow-up. No programme, session or payment is confirmed.</p></div><span class="pill">{{ $connection->status }}</span></article>@empty<div class="card empty"><h2>No bookings or connections yet</h2><p>Explore providers to find support for your child.</p><a href="{{ route('development.providers') }}">Browse providers →</a></div>@endforelse<section class="card lavender"><h2>Explore Programme Tracking</h2><p>See an example of session attendance, feedback and programme progress.</p><a href="{{ route('development.preview','tracking') }}">Preview tracking →</a></section>@endsection
